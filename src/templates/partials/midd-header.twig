<header class="midd-header{% if midd_header_transparent %} midd-header--transparent{% endif %}">
  <div class="midd-header__container">
    <div class="midd-header__primary">
      <div class="midd-header__logo">
        <a href="">
          <img src="images/midd-shield.svg" alt="" class="midd-header__shield">
          <img src="images/midd-wordmark.svg" alt="" class="midd-header__wordmark">
        </a>
      </div>

      <a href="#menu" data-toggle-target=".js-menu-1" class="menu-handle">
        <svg class="icon bars">
          <use xlink:href="#icon-bars">
        </svg>
        <svg class="icon times">
          <use xlink:href="#icon-times">
        </svg>

        <span class="menu-handle__text">Menu</span>
      </a>
    </div>

    <div class="midd-header__secondary js-menu-1">

      {% set nav_example = [
        { text: 'Our Schools' },
        { text: 'About Middlebury', items: ['A Sense of Purpose', 'The Exerpience of Place', 'Our Approach to Learning'] },
        { text: 'Offices and Services' },
      ] %}

      <nav class="midd-nav">
        <ul class="midd-nav__list">
          {% for item in nav_example %}
            <li class="midd-nav__item">
              <a href="" class="midd-nav__link">
                {{item.text}}
              </a>

              {% if item.items %}
                <a href="#" role="button" class="midd-nav__button" data-toggle-target=".js-middnav-list-{{loop.index}}">
                  <svg class="icon midd-nav__icon">
                    <use xlink:href="#icon-chevron-down">
                  </svg>
                </a>
                <ul class="midd-nav__list js-middnav-list-{{loop.index}}">
                  {% for subitem in item.items %}
                    <li class="midd-nav__item">
                      <a href="" class="midd-nav__link">{{subitem}}</a>
                    </li>
                  {% endfor %}
                </ul>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      </nav>

      <div class="midd-header__search dropdown">
        <div class="site-search dropdown">
          <a href="#search" class="site-search__handle">
            <svg class="icon"><use xlink:href="#icon-search"></svg> Search
          </a>

          <div class="site-search__content dropdown__menu dropdown__menu--stick-right">
            <form class="">
              <div class="site-search__group">
                <label for="midd-site-search" class="sr-only">Search</label>
                <input type="search" class="site-search__input" placeholder="Search" name="query" required id="midd-site-search">
                <button type="submit" class="site-search__submit">
                  <svg class="icon">
                    <use xlink:href="#icon-search">
                  </svg>
                </button>
              </div>
            </form>
          </div>
        </div>

      </div>
    </div>
  </div>

  {% block midd_header_content %}{% endblock %}

</header>
