<header class="page-header" aria-labelledby="midd-page-header-label">
  <div class="container">
    <div class="row">
      <div class="col-sm-10 offset-sm-1">

        {% include 'partials/breadcrumb.twig' with { items: breadcrumb } %}
        <h1 class="page-header__title" id="midd-page-header-label">{{title}}</h1>

      </div>
    </div>
  </div>
</header>

{# <div class="container">
  <div class="row">

    <div class="col-sm-10 offset-sm-1">
      <header class="page-header">
      {% include 'breadcrumb.twig' %}

        <h1 class='display-3 mb-1 mb-md-4'>Student Activities</h1>
      </header>
    </div>

  </div>
</div> #}
